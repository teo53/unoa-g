# ===========================================
# UNO A Flutter App - Environment Configuration
# ===========================================
#
# 이 파일을 .env로 복사하고 실제 값을 입력하세요:
# cp .env.example .env
#
# 주의: .env 파일은 절대 Git에 커밋하지 마세요!
# ===========================================

# ---------------------------------------------
# Supabase Configuration
# ---------------------------------------------
# Supabase 프로젝트 URL
# Supabase 대시보드 > Settings > API에서 확인
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous Key (공개 가능)
# Supabase 대시보드 > Settings > API > anon public key
SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key (서버 전용, 절대 클라이언트에 노출 금지!)
# Edge Functions에서만 사용
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---------------------------------------------
# TossPayments Configuration (결제)
# ---------------------------------------------
# TossPayments 클라이언트 키 (Flutter 앱에서 사용)
# 개발: test_ck_xxxx, 프로덕션: live_ck_xxxx
TOSSPAYMENTS_CLIENT_KEY=test_ck_your_client_key

# TossPayments 시크릿 키 (서버 전용, Edge Function에서만 사용)
# TOSSPAYMENTS_SECRET_KEY=test_sk_your_secret_key

# TossPayments Webhook 시크릿 (서명 검증용)
# TOSSPAYMENTS_WEBHOOK_SECRET=your_webhook_secret

# ---------------------------------------------
# OAuth Configuration (소셜 로그인)
# ---------------------------------------------
# Kakao OAuth
# KAKAO_CLIENT_ID=your_kakao_client_id
# KAKAO_CLIENT_SECRET=your_kakao_client_secret

# Google OAuth (Supabase Auth에서 설정)
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성

# Apple Sign In (iOS 전용)
# Apple Developer에서 Sign in with Apple 설정 필요

# ---------------------------------------------
# Environment Settings
# ---------------------------------------------
# development | staging | production
ENVIRONMENT=development

# 디버그 모드 (true/false)
DEBUG_MODE=true

# ---------------------------------------------
# App Configuration
# ---------------------------------------------
# Deep Link 스킴 (OAuth 콜백용)
APP_SCHEME=com.unoa.app

# API 타임아웃 (밀리초)
API_TIMEOUT=30000

# ---------------------------------------------
# Feature Flags (기능 플래그)
# ---------------------------------------------
# Mock 데이터 사용 여부 (개발용)
USE_MOCK_DATA=false

# 결제 테스트 모드
PAYMENT_TEST_MODE=true

# 푸시 알림 활성화
PUSH_NOTIFICATIONS_ENABLED=false

# ---------------------------------------------
# Analytics & Monitoring (선택사항)
# ---------------------------------------------
# Sentry DSN (에러 트래킹)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Firebase (분석)
# FIREBASE_PROJECT_ID=your-firebase-project

# ---------------------------------------------
# Development Only
# ---------------------------------------------
# 로컬 Supabase URL (supabase start 시)
# LOCAL_SUPABASE_URL=http://localhost:54321
# LOCAL_SUPABASE_ANON_KEY=your-local-anon-key
